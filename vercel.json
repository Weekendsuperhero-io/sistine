{
  "installCommand": "bun install --frozen-lockfile",
  "buildCommand": "bun run build-storybook && bun run export-storybook && bun run build",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "git": {
    "deploymentEnabled": false
  }
}
