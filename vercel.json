{
  "installCommand": "bun install --frozen-lockfile",
  "buildCommand": "bun run storybook:build && bun run storybook:export && bun run build",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "git": {
    "deploymentEnabled": false
  }
}
